name: Test Project
on: [push]
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: "Get code"
              uses: actions/checkout@v3
            - name: "Install NodeJS"
              uses: actions/setup-node@v3
              with:
                node-version: '18'
            - name: "Install dependencies"
              run: npm ci
            - name: "Run tests"
              run: npm test
    deploy:
        needs: test
        runs-on: ubuntu-latest
        steps:
            - name: "Get code"
              uses: actions/checkout@v3
            - name: "Install NodeJS"
              uses: actions/setup-node@v3
              with:
                node-version: '18'
            - name: "Move to directory"
              run: cd Project1
            - name: "Where I'm"
              run: "echo $PWD"
            - name: "Install dependencies"
              run: npm ci --workspace Project1
            - name: "Build project"
              run: npm run build
            - name: "Deploy"
              run: echo "Deploying..."